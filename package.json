{
  "name": "devcamper-api",
  "version": "1.0.0",
  "description": "Devcamper demo API",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server",
    "start": "cross-env NODE_ENV=production node server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "api"
  ],
  "author": "LuKa",
  "license": "MIT",
  "dependencies": {
    "cross-env": "^7.0.2",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.3"
  }
}
